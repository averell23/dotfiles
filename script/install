#!/usr/bin/env bash
#
# Run all dotfiles installers. Safe to run at any time to update.

set -e

cd "$(dirname "$0")"/..

HOSTNAME=$(hostname -s)

echo "› home-manager switch"
home-manager switch --flake ~/.dotfiles/nix#daniel@${HOSTNAME}

echo "› brew bundle"
brew bundle --file "homebrew/Brewfile.${HOSTNAME}"

# Run any remaining install.sh scripts (legacy)
find . -name install.sh | while read installer ; do sh -c "${installer}" ; done
